extends RActor;

p=6;
c=8;
while (true) {
    if (screenOut()) die();
    y-=6;
    var e=crashTo(Enemy);
    if (e) {
        e.die();
        die();
    }
    update();
}
\onUpdate() {
    //var t;
    c+=1;
    if (c>14) c=8;
}